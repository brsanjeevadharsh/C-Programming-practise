#include <stdio.h>
int main()
{
    char ch;
    scanf("%c",&ch);
    if(ch>=65 && ch<=91 || ch>=97 && ch<=123 || ch>=48 &&ch<=57)
    {
        printf("Give character is not special character");
    }
    else
    {
        printf("Given character is special character");
    }
}
