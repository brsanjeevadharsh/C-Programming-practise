#include <stdio.h>
int main()
{
    char str1[100];
    char str2[100];
    char str3[100];
   scanf("%[^\n]s",str1);
    printf("%s\n",str1);
    int num=0;
    for(int i=0;str1[i]!='\0';i++)
    {
        printf("%c\n",str1[i]);
        num++;
    }
    printf("%d",num);
    return 0;
}
