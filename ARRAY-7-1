#include <stdio.h>
int main()
{
    int n;
    printf("Enter the number of elements:");
    scanf("%d",&n);
    char c[n];
    for(int i=0;i<n;i++)
    {
        scanf(" %c",&c[i]);
    }
    for(int i=0;i<n;i++)
    {
        printf("%c",c[i]);
    }

    
}
