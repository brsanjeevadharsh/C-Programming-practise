//Program to calculate the sum of natural even numbers.

#include <stdio.h>
int main()
{
    int s=1;
    int e=10;
    int sum=0;
    while(s<=e)
    {
        if(s%2==0)
        {
            sum=sum+s;
        }
        s++;
    }
    printf("%d",sum);
}
