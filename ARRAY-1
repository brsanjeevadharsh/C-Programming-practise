#include <stdio.h>
int main()
{
    int n;
    printf("Enter the number of elements:");
    scanf("%d",&n);
    int elements[n];
    printf("Enter the elements:\n");
    for(int i=0;i<n;i++)
    {
        scanf("%d",&elements[i]);
    }
    printf("The elements in the array are:\n");
    for(int i=0;i<n;i++)
    {
        printf("%d\n",elements[i]);
    }
    return 0;
}
