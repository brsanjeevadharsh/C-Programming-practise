// Online C compiler to run C program online
#include <stdio.h>

int main() 
{
    int num;
printf("enter the number");
scanf("%d",&num);
if(num & 1!=0)
{
    printf("Odd number");
}
else
{
    printf("Even number");
}

    return 0;
}
