//To swap the elements inside an array
#include <stdio.h>
int main()
{
    int a[6]={1,2,3,4,5,6};
    int temp;
    for(int i=0;i<6;i+=2)
    {
        temp=a[i];
        a[i]=a[i+1];
        a[i+1]=temp;
    }
    for(int i=0;i<6;i++)
    {
        printf("%d",a[i]);
    }
}
